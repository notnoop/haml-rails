test:
  override:
    - nohup bash -c "avconv -f x11grab -r 30 -s 1280x1024 -i $DISPLAY -c:v libvpx -qmin 0 -qmax 50 -crf 10 -b:v 2M -threads 0 $CIRCLE_ARTIFACTS/asdf.webm &"
    - nothup bash -c "firefox &"
    - sleep 10
    - cat nohup.out
    - gem list
    - rake
    - bundle exec rake
    - which rake
    - bundle exec which rake
    - pkill avconv
